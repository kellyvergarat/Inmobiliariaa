const express = require('express');
const router =  express.Router();
const asesorCtrl = require('../controllers/asesor.controller');

//router.post('/create', asesorCtrl.create);

module.exports = router;