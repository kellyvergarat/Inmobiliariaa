const express = require('express');
const router =  express.Router();
const apartamentoCtrl = require('../controllers/apartamento.controller');

//router.post('/create', apartamentoCtrl.create);

module.exports = router;